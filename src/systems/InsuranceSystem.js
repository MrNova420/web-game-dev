/**
 * InsuranceSystem.js - Phase 6
 */
export class InsuranceSystem {
  constructor() { this.policies = new Map(); }
  buyInsurance(playerId, type) { console.log(`${playerId} bought ${type} insurance`); }
}
